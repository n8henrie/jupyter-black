[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[tool.black]
line-length = 79
target-version = ['py39']

[tool.isort]
profile = "black"
multi_line_output = 3
order_by_type = false
line_length = 79
